<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{ title }}</title>
  <meta name="description" content="{{ description }}">
  <link rel="canonical" href="{{ url }}" />
</head>
<body>
  <header>
    <h1>{{ title }}</h1>
  </header>
  <article>
  {{{ contents }}}
  </article>
  <footer>
     <p>This is an archived version coming originally from <a href="{{ url }}">{{ url }}</a> and archived using <a href="https://github.com/renoirb/archivator-demo">Archivator</a>.</p>
  </footer>

</body>
</html>
